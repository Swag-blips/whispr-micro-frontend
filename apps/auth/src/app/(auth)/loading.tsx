import { Loading } from "../components/icons";

const loading = () => {
  return (
    <div className="bg-[#0A0E0F] h-screen flex items-center justify-center">
      <Loading width="40" height="40" />
    </div>
  );
};

export default loading;
